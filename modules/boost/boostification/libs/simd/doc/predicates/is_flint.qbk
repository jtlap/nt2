[section is_flint]

[h5 Description]
Tests whether the input is an integer.

[h5 Synopsis]
    namespace boost { namespace simd
    {
      template <class A0> __unspecified__ is_flint(A0 const& a0);
    } }

[table Parameters
    [[Parameter] [Description]]
    [[a0]        [Operation's argument]]
]

[h5 Header]
   #include <boost/simd/toolbox/predicates/include/is_flint.hpp>
[h5 Expression Semantics]
   is_flint(a);

[*Return type: ] When operating on scalar types, the function returns a boolean, otherwise
                 when operating on SIMD types it returns a variable of a's type.


[*Semantics:   ]

[h5 Example]

[endsect]
