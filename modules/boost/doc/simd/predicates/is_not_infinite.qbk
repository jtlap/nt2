[section is_not_infinite]

[h5 Description]
Tests whether the input is not infinite.

[h5 Synopsis]
    namespace boost { namespace simd
    {
      template <class A0> __unspecified__ is_not_infinite(A0 const& a0);
    } }

[table Parameters
    [[Parameter] [Description]]
    [[a0]        [Operation's argument]]
]

[h5 Header]
   #include <boost/simd/toolbox/predicates/include/is_not_infinite.hpp>
[h5 Expression Semantics]
   is_not_infinite(a);

[*Return type: ] When operating on scalar types, the function returns a boolean, otherwise
                 when operating on SIMD types it returns a variable of a's type.


[*Semantics:   ]

[h5 Example]

[endsect]
